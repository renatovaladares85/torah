# Security Review Prompt

Review Torah as a restrictive-only GLPI security plugin. Check backend bypasses,
actor relation paths, contract and SLA/OLA paths, HTTP method, CSRF, ACL, entity
scope, active profile isolation, SQL safety, output escaping, disabled-plugin
behavior, and audit minimization. Reject any real, private, or identifying data.
Run the public-repository scanner and report residual risks before publication.

