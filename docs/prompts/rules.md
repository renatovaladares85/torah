# Rule Maintenance Prompt

When a new project rule is identified, add it first to `AGENTS.md` and to the
relevant prompt in this directory. If no existing prompt fits, create a focused
new prompt. Version and document that delivery before relying on the new rule.

